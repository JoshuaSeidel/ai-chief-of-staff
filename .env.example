# Environment variables for Docker deployment
# Note: API keys and database credentials are configured through the web UI, not here

# Application Configuration
NODE_ENV=production
PORT=3001

# Database Type (optional - can also be configured in UI)
DB_TYPE=sqlite

# If using PostgreSQL (can also be configured in UI)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=ai_chief_of_staff
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
