# Backend Environment Variables for Development
# Note: API keys are configured through the web UI, not here

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_TYPE=sqlite
DB_PATH=./data/ai-chief-of-staff.db

# If using PostgreSQL
# DB_TYPE=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=ai_chief_of_staff
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
